

function checarSePodeDirigir() {
  let idade = prompt("Qual a sua idade?");
 
 if (idade > 17) {
    alert("Já pode dirigir!");
}
 else {
    alert("Não pode dirigir ainda!");
    }
}

checarSePodeDirigir(18);

/* Rita, não sei se está correto. Tive bastante dificuldade em entender como criar o código em que o Alert de "Não pode dirigir"
aparecesse sempre. Espero que esteja correto.
*/
